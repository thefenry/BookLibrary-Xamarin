<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BookLibrary.Views.BookDetailPage"
             Title="Book Details">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Text:" FontSize="Medium" />
                <Label Text="{Binding Book.Title}" FontSize="Small"/>
                <Label Text="Author:" FontSize="Medium" />
                <Label Text="{Binding Book.Author}" FontSize="Small"/>
                <Label Text="Description:" FontSize="Medium" />
                <Label Text="{Binding Book.Description}" FontSize="Small"/>
                <Label Text="Series:" FontSize="Medium" />
                <Label Text="{Binding Book.Series}" FontSize="Small"/>
                <Label Text="Genre:" FontSize="Medium" />
                <Label Text="{Binding Book.Genre}" FontSize="Small"/>
                <Label Text="Category:" FontSize="Medium" />
                <Label Text="{Binding Book.Category}" FontSize="Small"/>
                <Label Text="EBook:" FontSize="Medium" />
                <Label Text="{Binding Book.IsEBook}" FontSize="Small"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" Clicked="EditItem_Clicked" />
        <ToolbarItem Text="Delete" Clicked="DeleteItem_Clicked" />
    </ContentPage.ToolbarItems>
</ContentPage>